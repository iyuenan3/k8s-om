# Default configure action is create.
# It could be used during configuring cluster network, tenant level resources, etc.
# Change to 'delete' or override variables through ansible command:
# ansible-playbook -i hosts configure-tenant.yml -e '{configure_action: delete}'
configure_action: create
